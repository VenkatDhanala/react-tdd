import React from 'react';
import { shallow } from 'enzyme'
import sinon from 'sinon'
import App from './App.js'
import AddPerson from './AddPerson'
import AddPersonList from './AddPersonList'

describe('Add Person', () => {
  
  it('if I click "Add" when there are empty fields, a warning message will appear.',()=>{
   
  })

  it('when I fill in the fields and click "Add", I am taken back to the list page.',() => {
    
   
  })

  it('Given that I have added a person, when I am on the list page, then I see my people',() => {
   

  })

})
